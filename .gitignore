node_modules/
# data (auth creds, message DB, media) lives in ~/.local/share/wa-cli — never in-repo,
# but ignore any stray local copies just in case:
data/
*.db
*.db-*
# local working notes / bug reports (may cite real numbers) — keep out of the public repo
BUG-REPORT-*.md
